{
  "name": "zootechxcrm",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm.cmd --prefix backend run dev\" \"npm.cmd --prefix frontend run dev\"",
    "build": "npm.cmd --prefix frontend run build",
    "start": "npm.cmd --prefix frontend run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
